<snippet>
	<content><![CDATA[
select t.${1:field}, count(*) as the_count
from ${2:table} t
group by t.$1
having count(*) > 1
order by 2 desc
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>sql-duplicate-value-check</tabTrigger>
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.sql</scope>
</snippet>
